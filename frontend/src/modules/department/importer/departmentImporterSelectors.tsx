import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const departmentImporterSelectors = importerSelectors(
  'department.importer',
);

export default departmentImporterSelectors;
