import colors from 'src/mui/assets/theme/base/colors';

const facingEnumerators = {
  type: ['Pusher', 'Hook'],
  typeColor: [colors.todo.main, colors.inprogress.main],
};

export default facingEnumerators;
